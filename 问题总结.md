# RoboCasa Agent 执行层问题总结

## 背景

毕业论文计划对比三种方案完成 RoboCasa 移动操作任务：
1. **Agent-based**：LLM（DeepSeek）做高层规划 + 技能库执行
2. **End-to-End VLA**：OpenVLA / Octo / GR00T N1.5 微调
3. **传统 BC**：Diffusion Policy

## 当前进展

LLM Agent 的**任务理解层面**已完美工作。DeepSeek 收到场景文字描述后，能正确将 "PickPlaceCounterToCabinet" 分解为 5-6 步：导航到目标物体 → 抓取 → 导航到目标设备 → 打开设备 → 放入 → 关闭 → 完成。每一步的参数（坐标、物体名）都能从场景描述中准确提取。

## 问题：Agent 无法执行任务（成功率 0%）

所有问题集中在**执行层**——即 LLM 下发的高层指令如何转化为仿真器中机器人的动作。

### 架构

```
DeepSeek → {"skill": "pick", "args": ["目标物体"]}
                ↓
SkillLibrary.pick() → 12维原始动作数组 → env.step(action)
                              ↑
                    [arm_delta_xyz(3), arm_delta_rot(3), gripper(1), base_vel(3), torso(1), mode(1)]
```

### 尝试方案 1：手写 P 控制器

用 Python 手写了导航和抓取的闭环控制器，直接下发 12 维原始动作指令。

**过程与发现**：

1. **动作布局搞反了 4 次**：PandaOmron 的 HybridMobileBase 控制器将 12 维动作按部件拆分（arm: 0-6, gripper: 6, base: 7-10, torso: 10, mode: 11）。通过直接读取 MuJoCo 的 `body_xpos` 验证每个维度的实际效果才确定正确索引。

2. **坐标系方向全部反了**：基座朝向世界坐标 -x 方向，导致手臂 x/y delta 和底盘速度的方向全部与世界坐标相反。需要加负号。

3. **手爪开合方向反了**：`action[6]=+1` 才是闭合（之前以为是 -1）。

4. **手臂移动极慢**：OSC_POSE 阻抗控制器（`impedance_mode=fixed`, `damping_ratio=1`）下，机械臂末端移动速度约 0.5mm/仿真步。移动 18cm 需要 371 步。

5. **最终瓶颈——抓取精度**：经过多轮迭代（见下表），手指始终无法降到物体高度以下。

| 迭代 | approach 高度 | 下降步数 | 手指-物体 z 差 | 物体位移 |
|------|-------------|---------|-------------|---------|
| 1 | obj_z + 18cm | 150 | +8.0cm | 0cm |
| 2 | obj_z + 8cm, target obj_z-4cm | 400 | +7.1cm | 1.7cm |
| 3 | 合并 approach+descend, 1500 步 | — | — | 0.8cm |
| 4 | obj_z+5cm + 800步对齐 + 1000步下降 | 1000 | +3.3cm | 7.5cm |
| 5 | obj_z+8cm + 2000步对齐 + 1500步下降 | 1500 | — | 未测试 |

手指 xy 误差 ~4.4cm，z 方向高出物体 ~3.3cm，物体被手指推动但无法夹稳。

**根因分析**：OSC_POSE 是**阻抗控制器**，设计目标是柔顺性（安全性）而非定位精度。其稳态误差约 4cm，而精密抓取需要 <1cm。再多的增量指令步数也无法消除这个物理层面的控制误差。

### 尝试方案 2：数据集轨迹重放

用 RoboCasa 已有的演示数据（LeRobot 格式）替换手写控制器。Agent 选择技能后，直接从数据集中加载对应任务的成功轨迹，逐帧回放 12 维动作。

**过程与发现**：

1. 数据集格式确认：每集 1 个 parquet 文件（12 维 action + 16 维 state），232 帧/集
2. 轨迹重放测试：加载 episode 0 的 action 序列，在新环境中逐帧执行
3. **失败**：环境布局不匹配。数据集的每条轨迹是在特定 `layout_id` 下录制的，但 `gym.make(seed=N)` 生成的布局与数据集中的布局不对应
4. 尝试 seed=0..4 匹配 episode 0，全部失败
5. 数据集 parquet 文件中不存储 `layout_id`，无法建立 episode→layout 映射

### 尝试方案 3：更换绝对位置控制器

OSC_POSE 控制器支持 `input_type="absolute"` 模式（直接下发末端绝对坐标，而非速度增量）。理论上可以消除增量累积误差。

**失败**：RoboCasa 的 `create_env()` 函数内部硬编码了 `controller_configs=controller_config`，无法从外部覆盖。直接使用 `robosuite.make()` 则不识别 RoboCasa 的厨房环境。

## 核心矛盾

RoboCasa 的设计假设底层控制来自**从数据中学习的策略**（BC/VLA），这些策略产出的动作序列与仿真器的控制器动力学是匹配的。而 Agent 方案试图从零手写开环控制器，面对的是：

- **阻抗控制器**的高稳态误差
- **全向移动底盘**的非线性动力学
- **随机化布局**导致的不可复现性

这三个因素叠加，使得手写控制器在合理时间内无法达到任务要求的执行精度。

## 可能的解决方向（寻求建议）

1. **从数据集中训练技能级小模型**：用 Behavior Cloning 或 Diffusion Policy 从数据集中学出单个技能（抓取、放置），Agent 只负责决定调用哪个技能和参数。Agent 做高层、小模型做低层。

2. **绕过 RoboCasa 的控制器限制**：修改 RoboCasa 源码，让 `create_env` 支持外部传入 `controller_configs`，使用绝对位置模式。或者使用 IK（逆运动学）直接从末端坐标算出关节角度。

3. **接受当前限制，重新定义 Agent 的角色**：Agent 不直接控制动作，而是从数据集中选择最匹配的参考轨迹，用 LLM 的推理能力做轨迹选择和参数适配。

4. **换一个对 Agent 更友好的仿真环境**：例如 LIBERO（固定基座，没有移动底盘）、CALVIN（桌面操作）、或更简单的 robosuite 自带任务（Lift、Stack 等）。

欢迎任何建议或指点。
